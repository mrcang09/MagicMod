# Knowledge Base

This folder keeps a lightweight snapshot of the changes and UI schema used in this mod.
It is intended for quick handoff to other agents.

The `files/` subtree is a point-in-time snapshot and may lag behind the live sources under `src/`.

See:
- `knowledge_base/purpose.md` for project goals.
- `knowledge_base/modification_steps.md` for recent change steps.
