# Project Purpose

- Provide a DragonCore-style, YAML-driven UI system for Forge 1.21.11.
- Allow fast iteration via `/open <ui.yml>` and resource reloads.
- Support layered elements, expressions for positioning, configurable fonts, actions, and overlays.
- Extend the UI runtime toward Bedrock-style model rendering so custom geometry can replace vanilla entity previews.
